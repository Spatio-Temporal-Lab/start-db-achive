<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.urbcomp.cupid.db.metadata.mapper.DatabaseMapper">
    <select id="selectAllByFid" resultType="org.urbcomp.cupid.db.metadata.entity.Database">
        SELECT *
        FROM sys_database
        WHERE user_id = #{fid} and delete_time = 0;
    </select>
    <select id="selectById" resultType="org.urbcomp.cupid.db.metadata.entity.Database">
        SELECT *
        FROM sys_database
        WHERE id = #{id} and delete_time = 0;
    </select>
    <select id="selectByFidAndName" resultType="org.urbcomp.cupid.db.metadata.entity.Database">
        SELECT *
        FROM sys_database
        WHERE user_id = #{fid} and name = #{name} and delete_time = 0;
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="org.urbcomp.cupid.db.metadata.entity.Database">
        INSERT INTO sys_database(`user_id`, `name`) VALUE (#{database.userId}, #{database.name});
    </insert>
    <update id="update">
        UPDATE sys_database
        SET name=#{database.name},
        user_id = #{database.userId}
        WHERE id = #{database.id} and delete_time = 0;
    </update>
    <delete id="deleteById">
        UPDATE sys_database
        SET delete_time = current_timestamp()
        WHERE id = #{id};
    </delete>
    <delete id="clean">
        DELETE
        FROM sys_database
        WHERE (delete_time != 0 AND TIMESTAMPDIFF(SECOND,delete_time, current_timestamp()) > #{expiredTimeLenS})
        OR (user_id in (SELECT id FROM sys_user WHERE delete_time != 0))
    </delete>
</mapper>
